export interface elementInfo {
  elements: number;
  currentElement: number;
  currentElementY: number;
  elementHeight: number;
}
export interface positionOnPage {
  previousElement: number;
  nextElement: number;
  scrollValue: number;
}
